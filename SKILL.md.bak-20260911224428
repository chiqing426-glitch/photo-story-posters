---
name: photo-story-posters
description: 将用户上传的照片制作或修改为高级照片叙事海报。用户提到潘通相框海报、潘通色卡海报、Pantone photo poster、主体自然穿出白色相框、从照片提取主色并标注潘通近似色号，或继续修改这一系列海报时使用。
---

# 制作照片叙事海报

把用户提供的照片制作成高级编辑感海报：白色相框、克制的主体穿框、低饱和背景和潘通式色号标签。始终保留真实摄影质感，并在后续修改中锁定用户已经确认的效果。

## 先判断平台能力

1. 平台具备图片编辑能力时，优先编辑用户原图，不要重绘关键主体。
2. 平台只有图片生成功能时，把用户原图作为图像参考，最大限度保留主体身份、结构、透视和真实纹理。
3. 平台无法直接生成或编辑图片时，输出一段可复制到该平台图片工具中的完整生成提示词，不要声称已经完成图片。
4. 用户没有上传原图时，先请用户上传；若用户只想要模板或提示词，则直接输出模板。

## 多图系列模式

当用户上传两张或以上图片时，先统计图片数量，并询问：“您上传了 [数量] 张图片，希望把它们拼成一张海报吗？请选择：拼成一张／分别制作成系列海报。”不要在用户回答前裁切或生成正式海报。明确图片角色；没有指定时按视觉内容判断并告知用户。

### 用户选择“拼成一张”

1. 判断网格、上下/左右分栏、主图加辅图或不规则版式。
2. 输出真正的视觉布局线稿预览：使用线条、矩形框、编号、箭头和简短中文标签绘制一张可直接查看的示意图，而不是只输出字符框。线稿须标出画布比例、图片编号、位置、占比和阅读顺序；图像生成不可用时才退回等宽文本框或 Mermaid，并明确这是示意。
3. 若三张图包含制作过程与成品，默认把成品图放在右侧大幅画面，过程图放在左侧上下小幅画面；只有用户指定其他主次关系时才改变。
3. 同时给出每张图的裁切范围（方向校正后原图的左、上、右、下百分比和原图尺寸）、保留主体、舍弃元素及理由。
4. 用户确认布局和裁切后，按确认方案拼图并统一相框、背景、边距、字体、色板和输出比例。
5. 检查编号顺序、裁切、比例、接缝、风格和文字。

### 用户选择“分别制作成系列海报”

1. 为每张图输出编号、主体、干扰元素和拟裁切范围；默认统一画布比例、相框、边距、字体、标签位置和背景处理。
2. 用户确认整组裁切后逐张生成，保持系列模板一致、每张图保留独立配色。
3. 输出系列总览，标注裁切和生成式细节变化。

用户已明确拼接或分别制作时跳过询问，直接进入预览；只说“处理这几张图”时必须先询问。单图继续现有流程。

## 穿框方案确认

在正式生成单图海报或多图拼接海报前，必须单独确认是否使用穿框效果。结合画面重心，先说明拟冲出相框的元素、方向、进入区域、预计比例，以及是否可能遮挡标题、色号或其他图片，然后提供三个选择：

1. 冲出相框；
2. 不冲出相框；
3. 调整冲出重心。

用户选择“冲出相框”后，锁定穿框元素、方向和范围，并在生成提示词中明确要求主体跨越照片内框边缘，形成真实的前后层级、透视和接触阴影。穿框面积控制在主体的 5%–15%，且不得触碰最终画布外边缘。

用户选择“不冲出相框”后，禁止添加任何穿框元素，保持主体完全位于照片内框内。用户选择“调整冲出重心”后，重新展示穿框位置示意，等待确认后再生成。未获用户确认时不得自行添加穿框效果。

## 画面重心与裁切预览

制作海报前，先确定照片值得保留的视觉主体。保留主体身份与真实纹理，不等于保留整张原图的所有区域。
- 综合用户意图、视觉显著性、纹理、光线和叙事作用识别主角；不要把最亮或最大区域机械地当作主体。
- 将其余元素分为必要环境与干扰。不要默认删除所有人物、建筑或背景；先看它们是否帮助表达主题。
- 优先以裁切、调整主体位置和框内照片比例解决干扰；只有裁切无法解决时，才建议局部清理或补画。不要为维持原图比例凭空生成场景。
- 区分框内照片比例和最终海报比例；未指定尺寸时按主体选择，不强行沿用长竖原图。

三种模式：
1. 保留原构图：用户要求保留全图时，只包装海报。
2. 自动突出主体：用户明确授权自动裁切并直接制作时，自行选择裁切范围并出图。
3. 先看裁切预览（默认）：先展示原图上的拟裁切区域或独立裁切预览，简述保留和舍弃内容，等用户确认再生成正式海报。用户已明确裁切范围并要求生成时，不重复确认。

裁切预览须基于原图，记录相对于经方向校正后原图的左、上、右、下百分比，以及原图尺寸。遵守当前环境的图像编辑工具规则；无法精确裁切时标明预览是示意，不声称像素级保真。没有预览工具时给出坐标和理由，不虚构已生成的预览。
确认后把裁切范围、保留主体和输出比例写进生成提示词，修改时沿用已确认范围。

示例：鱼鳞云照片中的底部岗亭、标牌、树木和右侧楼体若没有叙事作用，可裁取上方纯天空，保留云纹疏密与明暗变化。此例不意味着所有天空照片都应去除地景。

## 标准流程

1. 检查原始照片、模板图和用户的文字要求。
2. 按画面重心流程选择并确认构图，再从保留区域识别一至四种主色。
3. 按色号规则选择标签模式；无可靠色库依据时使用近似 HEX 配色，不编造潘通匹配。
4. 使用当前平台可用的图片编辑或生成功能完成海报；能编辑原图时不要从零重绘主体。
5. 交付前逐项执行最终检查。

需要生成、局部修改或去除文字重影时，读取 [references/prompt-recipes.md](references/prompt-recipes.md)。

## 构图规则

- 保持原照片清晰、真实且可识别。除非用户要求，否则不要模糊、雾化、过度提饱和、改色或改变曝光。
- 外部背景从照片主色中提取，优先使用高明度、低饱和色和柔和渐变，避免生硬的双色分界。
- 使用白色或暖白色潘通卡片式相框，并在底部保留充足的文字区域。
- 建立背景、框内照片和框前主体三层关系；只让少量关键轮廓自然穿出内框。
- 穿框效果必须克制，服务于空间层次，不得大面积遮挡相框或显得浮夸。
- 穿框元素与画布外边缘之间必须保留明显安全留白；叶片、水花、云、花瓣、树枝和液体均不得意外贴边或被裁切。
- 主体穿框处只添加轻微、可信的接触阴影；相框自身的投影也要克制。
- 保留有意设计的留白。用户要求主体在右、左侧留白时，左侧必须干净且可用于排版。
- 严格遵守指定的比例和像素尺寸，包括 `1125 × 633 px` 等横版规格。

## 不同主体的处理

- **荷花和叶片：** 保留花瓣纹理与叶脉清晰度；只选少量荷叶或花瓣置于相框前，其余留在框内或框后；每个交叠都要有明确层级。
- **海浪和沙滩：** 可让少量浪尖与海沫跨出内框；保持海平线稳定，并在左右两侧保留安全留白。
- **云层和晚霞：** 云缘应断续、松散、自然，并具有真实的雾感层次；避免对称棉花团、僵硬轮廓光和过多穿框云朵。
- **花朵和树枝：** 仅在用户要求或确实影响画面时清理杂乱元素；保留主干、代表性花朵和自然生长方向。
- **汽车和液体：** 保持机械结构可信；液流可以穿框，但顶部和两侧必须留白，流动形态要符合物理直觉。
- **其他主体：** 只选择最清楚的轮廓或动作穿框，不要强迫所有元素产生穿框效果。

## 色号与文字

- 选择一至四种能代表照片主要构成元素的颜色。
- 默认使用“HEX 色值 + 简洁色名”，区分实际像素采样和视觉估计；未实际取样不得称为精确提色。色名可以是描述性命名。
- 用户要求潘通编号时，仅采用用户给定色号或可追溯色库中的候选。未核实编号与色样关系时不要伪造匹配；可先保留待匹配标签。
- 潘通候选注明视觉近似，非实体色卡校准。自拟英文色名不得宣称是潘通官方名称。
- 仅在用户模板或要求需要时使用 `PANTONE®` 主标题。
- 模仿参考图的无衬线字体层级、字距和对齐，不要添加装饰性字体效果。
- 使用四组颜色时，默认分成左右两列，每列两个色号；若模板另有版式则遵循模板。
- 所有文字必须是单层、锐利、纯黑的平面印刷效果。
- 禁止文字倒影、重复文字、投影、浮雕、斜面、光晕、模糊重影或描边光。
- 用户要求用品牌 Logo 替换 `PANTONE®` 时，必须保留原 Logo 的字形、比例和颜色，并按用户要求控制视觉尺寸。

## 修改纪律

- 新要求没有涉及的已确认效果全部锁定，不得被重新生成或改变。
- 局部问题只修改最小必要区域，不要重做无关部分。
- 将百分比坐标理解为相对于完整画布的位置，并围绕该点进行局部调整。
- 每次局部修订后重新检查安全留白、图层顺序、文字锐利度和背景颜色。
- 不得重新引入此前已去除的文字倒影、杂物、模糊或错误穿框关系。

## 最终检查

- 原始主体清晰、真实、可识别。
- 背景、框内照片、相框和框前主体的层级自然明确。
- 穿框元素克制，且不触碰画布外边缘。
- 背景与照片协调，并保持明亮、低饱和。
- 底部文字区留白充足，对齐平衡。
- 配色标签清楚、布局正确，HEX 或潘通模式与依据一致。
- 文字为单层锐利纯黑，无倒影、重影或投影。
- 输出比例与像素尺寸准确。
- 所有已确认效果均被保留。
- 仅在实际使用潘通近似编号时说明：`潘通色号为依据画面进行的视觉近似匹配，并非实体色卡校准值。`

- 已确认裁切范围得到执行，干扰元素未被重新引入。生成式编辑引起的细节变化应如实说明。
- 不自动附加作者署名、推广语或非商业用途尾注。来源信息保存在 SOURCE.md，输出文案修改不代表授权范围改变。
